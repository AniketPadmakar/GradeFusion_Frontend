// const hostURL = {
//     link:
//       process.env.NODE_ENV === "production"
//         ? "//backend deployment url"
//         : "http://localhost:4000",
//   };
  
//   export default hostURL;
  const hostURL = {
    link:"http://localhost:4000"
  };
  
  export default hostURL;
  